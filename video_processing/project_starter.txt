Install pyenv 
brew install pyenv 

Install python 3.12.3
pyenv install 3.12.3

Set it as global or local
pyenv global 3.12.3

check python3 --version 
if still at 3.10 then make change in ~/.zshrc
nano ~/.zshrc
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init --path)"
eval "$(pyenv init -)"

create venv 
python -m video_processing_env venv
source video_processing_env/bin/activate

